import { styled } from '@mui/material'
import ButtonColumn from './ButtonColumn'

const StyledButtonColumn = styled(ButtonColumn)(() => ({
    border: '2px solid #3f51b5',
    textTransform: 'lowercase'
}))

export default StyledButtonColumn
